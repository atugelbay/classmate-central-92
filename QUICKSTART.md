# Quick Start Guide

## Шаг 1: Запуск базы данных

```bash
cd backend
docker-compose up -d
```

Проверьте что PostgreSQL запущен:
```bash
docker-compose ps
```

## Шаг 2: Запуск Backend

```bash
# В папке backend
go run cmd/api/main.go
```

Вы должны увидеть:
```
Successfully connected to database
Migrations executed successfully
Server starting on port 8080
```

Backend API: `http://localhost:8080`

## Шаг 3: Запуск Frontend

Откройте новый терминал:

```bash
cd frontend
npm install  # Первый раз
npm run dev
```

Frontend: `http://localhost:5173`

## Шаг 4: Первый вход

1. Откройте браузер: `http://localhost:5173`
2. Вы будете перенаправлены на страницу входа
3. Нажмите "Зарегистрироваться"
4. Заполните форму:
   - Имя: Ваше имя
   - Email: admin@example.com
   - Пароль: минимум 6 символов
5. После регистрации вы автоматически войдете

## Шаг 5: Использование системы

### Добавить учителя
1. Перейдите в "Учителя"
2. Нажмите "Добавить учителя"
3. Заполните форму

### Добавить ученика
1. Перейдите в "Ученики"
2. Нажмите "Добавить ученика"
3. Заполните форму

### Создать группу
1. Перейдите в "Группы"
2. Нажмите "Создать группу"
3. Выберите учителя и заполните данные

### Создать урок
1. Перейдите в "Расписание"
2. Нажмите "Добавить урок" или кликните на календаре
3. Заполните информацию об уроке

## Проверка работы API

```bash
# Health check
curl http://localhost:8080/health

# Ответ должен быть: {"status":"ok"}
```

## Остановка

### Backend
Нажмите `Ctrl+C` в терминале с backend

### Frontend
Нажмите `Ctrl+C` в терминале с frontend

### Database
```bash
cd backend
docker-compose down
```

## Troubleshooting

### Порт 8080 уже занят
Измените `SERVER_PORT` в `backend/.env`

### Порт 5173 уже занят
Frontend автоматически предложит другой порт

### База данных не подключается
```bash
cd backend
docker-compose down
docker-compose up -d
# Подождите 5 секунд
go run cmd/api/main.go
```

### Ошибка "Cannot connect to API"
1. Убедитесь что backend запущен
2. Проверьте `frontend/.env` - должен быть `VITE_API_URL=http://localhost:8080/api`
3. Перезапустите frontend после изменения .env

## Готово!

Теперь у вас работающая система управления образовательным центром! 🎉

