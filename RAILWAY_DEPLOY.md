# 🚂 Деплой на Railway.app

## Пошаговая инструкция

### Шаг 1: Подготовка проекта (✅ Готово!)

Все необходимые файлы уже созданы:
- `backend/Dockerfile` - для сборки Go приложения
- `frontend/Dockerfile` - для сборки React приложения с Nginx
- `frontend/nginx.conf` - конфигурация веб-сервера
- `railway.json` - конфигурация Railway
- `.dockerignore` файлы для оптимизации сборки

---

### Шаг 2: Загрузка на GitHub (если еще не сделано)

```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте коммит
git commit -m "Prepare for Railway deployment"

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/ВАШ_USERNAME/classmate-central.git

# Отправьте код
git push -u origin main
```

---

### Шаг 3: Регистрация на Railway

1. Перейдите на **[railway.app](https://railway.app)**
2. Нажмите **"Login with GitHub"**
3. Авторизуйте Railway для доступа к вашим репозиториям

---

### Шаг 4: Создание проекта

1. На главной странице нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите и выберите ваш репозиторий `classmate-central`
4. Railway начнет автоматическое развертывание

---

### Шаг 5: Настройка PostgreSQL базы данных

1. В вашем Railway проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных
4. Переменная `DATABASE_URL` будет автоматически доступна

---

### Шаг 6: Создание двух сервисов

Railway нужно настроить для запуска frontend и backend отдельно.

#### Backend Service:

1. Нажмите на ваш проект
2. Перейдите в **Settings** → **General**
3. Установите **Root Directory**: `backend`
4. В разделе **Variables** добавьте:
   ```
   GIN_MODE=release
   PORT=8080
   ```
5. В разделе **Networking** включите **Public Networking**
6. Скопируйте URL (например: `https://backend-production-xyz.up.railway.app`)

#### Frontend Service:

1. Нажмите **"+ New"** → **"GitHub Repo"** (выберите тот же репозиторий)
2. В **Settings** → **General**
3. Установите **Root Directory**: `frontend`
4. В разделе **Variables** добавьте:
   ```
   VITE_API_URL=https://ваш-backend-url.up.railway.app
   ```
5. В разделе **Networking** включите **Public Networking**
6. Скопируйте URL (например: `https://frontend-production-abc.up.railway.app`)

---

### Шаг 7: Переменные окружения для Backend

В настройках Backend сервиса добавьте переменные:

```bash
# Railway автоматически предоставит DATABASE_URL
# Вам нужно добавить только:

JWT_SECRET=ваш_супер_секретный_ключ_минимум_32_символа
GIN_MODE=release
PORT=8080
```

Для генерации JWT_SECRET можно использовать:
```bash
openssl rand -base64 64
```

---

### Шаг 8: Обновление CORS в Backend

Убедитесь, что в `backend/internal/middleware/cors.go` разрешены запросы с вашего frontend URL:

```go
config := cors.DefaultConfig()
config.AllowOrigins = []string{
    "http://localhost:5173",
    "https://ваш-frontend-url.up.railway.app", // Добавьте ваш Railway URL
}
```

---

### Шаг 9: Deploy!

1. Railway автоматически будет пересобирать при каждом `git push`
2. Следите за логами в Railway Dashboard
3. После успешного деплоя откройте ваш frontend URL

---

## 🔧 Полезные команды

### Просмотр логов:
- Откройте сервис в Railway Dashboard
- Перейдите во вкладку **"Deployments"**
- Нажмите на последний деплой для просмотра логов

### Перезапуск сервиса:
- В настройках сервиса нажмите **"Restart"**

### Откат к предыдущей версии:
- Во вкладке **"Deployments"** найдите нужную версию
- Нажмите **"Redeploy"**

---

## 💰 Стоимость

Railway предоставляет:
- **$5 бесплатных кредитов** каждый месяц
- Этого достаточно для тестирования (примерно 500 часов работы маленького приложения)
- PostgreSQL база включена

---

## 📊 Мониторинг

В Railway Dashboard вы можете видеть:
- Использование CPU и памяти
- Количество запросов
- Логи в реальном времени
- Метрики базы данных

---

## 🐛 Troubleshooting

### Backend не запускается:
```bash
# Проверьте логи в Railway Dashboard
# Убедитесь что DATABASE_URL настроен
# Проверьте что миграции выполнились
```

### Frontend показывает ошибки API:
```bash
# Убедитесь что VITE_API_URL правильно настроен
# Проверьте CORS в backend
# Убедитесь что backend сервис запущен
```

### База данных не подключается:
```bash
# Убедитесь что PostgreSQL сервис создан
# Проверьте что DATABASE_URL автоматически добавлен в переменные backend
```

---

## 🚀 Готово!

Теперь ваше приложение доступно в интернете по адресу:
- Frontend: `https://ваш-frontend.up.railway.app`
- Backend API: `https://ваш-backend.up.railway.app`

Можете делиться ссылкой с коллегами и тестировать! 🎉

