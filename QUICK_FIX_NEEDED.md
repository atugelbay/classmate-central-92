# 🔥 Быстрый фикс: 500 ошибки на финансах

## Проблемные endpoints:
- ❌ `/api/tariffs` - 500
- ❌ `/api/payments/transactions` - 500
- ❌ `/api/debts` - 500
- ✅ `/api/payments/balances` - 200 (уже исправлен)

## Возможные причины:

### 1. В БД нет колонки `company_id` в таблицах:
- `tariffs`
- `payment_transactions`
- `debt_records`

### 2. Или не применена SQL миграция `006_add_multi_tenancy.up.sql`

## Решение:

Нужно применить миграцию в БД:
```bash
psql -U postgres -d classmate_central -f backend/migrations/006_add_multi_tenancy.up.sql
```

Или перезапустить БД с миграциями.

---

**Проверяю сейчас что именно...**

