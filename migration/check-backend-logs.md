# 🔍 Дебаг: Проверь логи backend

## В терминале где запущен backend:

Посмотри что выводится когда ты делаешь запрос на `/api/students`.

Должно быть примерно так:

```
[GIN] 2025/10/26 - 12:XX:XX | 200 |    15.4521ms |             ::1 | GET      "/api/students"
```

Если видишь **401** или **500** - это проблема.

---

## Также проверь:

1. **Backend скомпилирован с новым кодом?**
   ```bash
   cd backend
   go build -o bin/api.exe ./cmd/api
   ```

2. **Middleware применен?**
   Проверь что в `main.go` есть:
   ```go
   api.Use(middleware.CompanyMiddleware(db.DB))
   ```

3. **User Repository обновлен?**
   Проверь что в `user_repository.go` запросы включают `company_id`.

---

**Скажи что показывают логи backend при запросе!**

