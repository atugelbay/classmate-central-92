# 📦 Руководство по миграции данных из AlfaCRM

## Обзор

Система Classmate Central поддерживает миграцию данных из AlfaCRM через удобный веб-интерфейс. Все данные будут автоматически импортированы в вашу компанию.

## Что мигрируется

✅ **Учителя** (Teachers)  
✅ **Студенты** (Students) + балансы  
✅ **Группы** (Groups)  
✅ **Аудитории** (Rooms)  
✅ **Типы абонементов** (Subscription Types)  
✅ **Абонементы студентов** (Student Subscriptions)  
✅ **Расписание групп** (Group Schedules)  
✅ **Уроки** (Lessons - 2 недели вперед)  
✅ **Транзакции** (Payment Transactions)  
✅ **Долги** (Debt Records)

## Шаги по миграции

### 1. Получите данные из AlfaCRM

Вам понадобятся:

- **URL вашей AlfaCRM**: например `https://yourcompany.s20.online`
- **Email администратора**
- **API ключ**: находится в настройках AlfaCRM → API → Ключ

### 2. Откройте страницу миграции

1. Войдите в систему Classmate Central
2. Перейдите в **Настройки** (Settings)
3. Откройте вкладку **Миграция**

### 3. Заполните форму

Введите:
- URL вашей AlfaCRM системы
- Email администратора
- API ключ

### 4. Проверьте соединение

Нажмите кнопку **"Проверить соединение"**

- ✅ Если соединение установлено - продолжайте
- ❌ Если ошибка - проверьте введенные данные

### 5. Запустите миграцию

Нажмите **"Запустить миграцию"**

- Процесс займет несколько минут (зависит от объема данных)
- Не закрывайте страницу во время миграции
- Вы увидите прогресс-бар и текущий шаг

### 6. Проверьте результаты

После завершения вы увидите статистику:
- Количество импортированных учителей
- Количество студентов
- Количество групп
- И другие данные

## Важные замечания

⚠️ **Обязательно прочитайте:**

1. **Повторная миграция** - если вы запустите миграцию повторно, существующие данные будут обновлены (не дублированы)

2. **Multi-tenancy** - все данные привязываются к вашей компании автоматически. Другие пользователи не увидят ваши данные

3. **Время миграции** - может занять от 2 до 10 минут в зависимости от количества данных

4. **Не закрывайте страницу** - миграция запускается в фоне, но статус обновляется только когда страница открыта

5. **Уроки** - генерируются на 2 недели вперед от текущей даты

## API Endpoints

Если хотите использовать миграцию программно:

### POST `/api/migration/test-connection`
Проверка подключения к AlfaCRM

```json
{
  "alfacrmUrl": "https://yourcompany.s20.online",
  "email": "admin@example.com",
  "apiKey": "your-api-key"
}
```

### POST `/api/migration/start`
Запуск миграции

```json
{
  "alfacrmUrl": "https://yourcompany.s20.online",
  "email": "admin@example.com",
  "apiKey": "your-api-key",
  "migrateRooms": true,
  "migrateLessons": true
}
```

### GET `/api/migration/status`
Получение статуса миграции

Ответ:
```json
{
  "status": "running|completed|failed",
  "currentStep": "Миграция студентов",
  "progress": 45,
  "teachersCount": 7,
  "studentsCount": 262,
  "groupsCount": 53,
  "roomsCount": 12,
  "lessonsCount": 172,
  "startedAt": "2025-10-26T14:00:00Z"
}
```

## Troubleshooting

### Ошибка подключения

**Проблема:** "Failed to connect to AlfaCRM"

**Решение:**
- Проверьте правильность URL (должен начинаться с `https://`)
- Убедитесь что API ключ актуальный
- Проверьте доступность AlfaCRM системы

### Миграция застряла

**Проблема:** Прогресс не обновляется

**Решение:**
- Обновите страницу и проверьте статус
- Проверьте логи бэкенда
- Перезапустите миграцию

### Дубликаты данных

**Проблема:** После повторной миграции данные дублируются

**Решение:**
- Миграция использует `ON CONFLICT` для предотвращения дублирования
- Если дубликаты появились, обратитесь к администратору базы данных

## Технические детали

### Архитектура

```
Frontend (React)
  ↓
API Endpoints (Go)
  ↓
Migration Handler
  ↓
Node.js Migration Script
  ↓
AlfaCRM API
  ↓
PostgreSQL Database
```

### Порядок миграции

1. Создание компании (если не существует)
2. Преподаватели
3. Аудитории
4. Типы абонементов
5. Группы
6. Расписания групп
7. Студенты + балансы
8. Абонементы студентов
9. Транзакции
10. Долги
11. Генерация уроков

### Конвертация времени

Все времена из AlfaCRM (Almaty Time, UTC+5) автоматически конвертируются в UTC для хранения в базе данных.

## Поддержка

Если у вас возникли проблемы с миграцией:

1. Проверьте логи бэкенда (`backend/error.log`)
2. Убедитесь что все миграции базы данных применены
3. Проверьте права доступа в AlfaCRM

---

**Версия:** 1.0.0  
**Последнее обновление:** Октябрь 2025

