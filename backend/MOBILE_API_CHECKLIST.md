# Чеклист API покрытия для мобильного приложения

## ✅ Реализованные endpoints

### Аутентификация
- [x] POST `/api/auth/login` - Вход
- [x] POST `/api/auth/refresh` - Обновление токена
- [x] GET `/api/auth/me` - Текущий пользователь
- [x] POST `/api/auth/register` - Регистрация
- [x] POST `/api/auth/verify-email` - Подтверждение email
- [x] POST `/api/auth/accept-invite` - Принятие приглашения

### Студенты
- [x] GET `/api/students` - Список студентов (с пагинацией и поиском)
- [x] GET `/api/students/:id` - Детали студента
- [x] POST `/api/students` - Создание студента
- [x] PUT `/api/students/:id` - Обновление студента
- [x] DELETE `/api/students/:id` - Удаление студента
- [x] GET `/api/students/:id/notifications` - Уведомления студента
- [x] GET `/api/students/:id/attendance` - Журнал посещаемости
- [x] GET `/api/students/:id/activities` - Активности студента

### Расписание (Lessons)
- [x] GET `/api/lessons` - Все уроки (с фильтрацией по датам)
- [x] GET `/api/lessons/:id` - Детали урока
- [x] GET `/api/lessons/individual` - Индивидуальные уроки
- [x] GET `/api/lessons/teacher/:teacherId` - Уроки преподавателя
- [x] POST `/api/lessons` - Создание урока
- [x] PUT `/api/lessons/:id` - Обновление урока
- [x] DELETE `/api/lessons/:id` - Удаление урока

### Посещаемость (Attendance)
- [x] POST `/api/attendance` - Отметка посещаемости
- [x] GET `/api/attendance/lesson/:lessonId` - Посещаемость по уроку
- [x] GET `/api/attendance/student/:studentId` - Посещаемость студента

### Финансы
- [x] GET `/api/payments/transactions` - Все транзакции
- [x] GET `/api/payments/transactions/student/:studentId` - Транзакции студента
- [x] POST `/api/payments/transactions` - Создание транзакции
- [x] PUT `/api/payments/transactions/:id` - Обновление транзакции
- [x] GET `/api/payments/balance/:studentId` - Баланс студента
- [x] GET `/api/payments/balances` - Все балансы
- [x] GET `/api/debts` - Долги (с фильтрацией по статусу)
- [x] GET `/api/debts/student/:studentId` - Долги студента

### Абонементы (Subscriptions)
- [x] GET `/api/subscriptions` - Все абонементы
- [x] GET `/api/subscriptions/student/:studentId` - Абонементы студента
- [x] GET `/api/subscriptions/:id` - Детали абонемента
- [x] POST `/api/subscriptions` - Создание абонемента
- [x] PUT `/api/subscriptions/:id` - Обновление абонемента
- [x] DELETE `/api/subscriptions/:id` - Удаление абонемента
- [x] POST `/api/subscriptions/:id/freeze` - Заморозка абонемента
- [x] GET `/api/subscriptions/:id/freezes` - История заморозок

### Группы
- [x] GET `/api/groups` - Все группы
- [x] GET `/api/groups/:id` - Детали группы
- [x] POST `/api/groups` - Создание группы
- [x] PUT `/api/groups/:id` - Обновление группы
- [x] DELETE `/api/groups/:id` - Удаление группы

### Преподаватели
- [x] GET `/api/teachers` - Все преподаватели
- [x] GET `/api/teachers/:id` - Детали преподавателя
- [x] POST `/api/teachers` - Создание преподавателя
- [x] PUT `/api/teachers/:id` - Обновление преподавателя
- [x] DELETE `/api/teachers/:id` - Удаление преподавателя

### Настройки
- [x] GET `/api/settings` - Получить настройки
- [x] PUT `/api/settings` - Обновить настройки

### Дашборд
- [x] GET `/api/dashboard/stats` - Общая статистика
- [x] GET `/api/dashboard/today-lessons` - Уроки на сегодня
- [x] GET `/api/dashboard/revenue-chart` - График доходов
- [x] GET `/api/dashboard/attendance-stats` - Статистика посещаемости

### Уведомления
- [x] PUT `/api/notifications/:notificationId/read` - Отметить как прочитанное

### Health Checks
- [x] GET `/health` - Проверка здоровья сервера
- [x] GET `/ready` - Проверка готовности (readiness probe)

## 🔄 Особенности реализации

### Аутентификация
- JWT токены с refresh механизмом
- Токены содержат информацию о ролях и правах доступа
- Автоматическое обновление токенов через middleware

### Пагинация
- Поддерживается на всех endpoints со списками
- Параметры: `page`, `pageSize`
- Ответ включает: `total`, `page`, `pageSize`, `totalPages`

### Фильтрация
- Поиск через параметр `query`
- Фильтрация по датам: `startDate`, `endDate`
- Фильтрация по статусам: `status`

### Права доступа (RBAC)
- Каждый endpoint защищен проверкой прав
- Права возвращаются в токене пользователя
- Ошибка 403 при отсутствии прав

### Мультитенантность
- Все данные изолированы по `company_id`
- Автоматическое добавление `company_id` через middleware
- Пользователь видит только данные своей компании

## 📱 Рекомендации для мобильного приложения

### Кэширование
- Кэшируйте данные студентов, групп, преподавателей
- Обновляйте кэш при изменении данных
- Используйте ETags для оптимизации запросов (если будет реализовано)

### Офлайн режим
- Сохраняйте последние данные локально
- Синхронизируйте при восстановлении соединения
- Очередь для операций создания/обновления

### Push-уведомления
- Интегрируйте с Firebase/APNs для push-уведомлений
- Используйте `/api/students/:id/notifications` для получения уведомлений
- Реализуйте polling или WebSocket для real-time обновлений (если будет реализовано)

### Оптимизация запросов
- Используйте пагинацию для больших списков
- Запрашивайте только необходимые поля (если будет реализован GraphQL или field selection)
- Кэшируйте настройки и справочники

### Обработка ошибок
- Всегда проверяйте HTTP статус коды
- Обрабатывайте 401 (неавторизован) - обновляйте токен
- Обрабатывайте 403 (нет доступа) - показывайте сообщение пользователю
- Обрабатывайте 500 (ошибка сервера) - логируйте и показывайте сообщение

### Безопасность
- Храните токены в Keychain/Keystore
- Используйте HTTPS для всех запросов
- Не логируйте токены и пароли
- Валидируйте данные перед отправкой

## 🚀 Следующие шаги

1. **WebSocket поддержка** (опционально)
   - Real-time обновления уведомлений
   - Real-time обновления посещаемости

2. **GraphQL API** (опционально)
   - Более гибкие запросы
   - Меньше over-fetching

3. **Batch операции**
   - Массовая отметка посещаемости
   - Массовое создание уроков

4. **Файлы и медиа**
   - Загрузка аватаров
   - Загрузка документов

5. **Офлайн синхронизация**
   - Conflict resolution
   - Optimistic updates

