# 🧪 Тест миграции - исправлено!

## Что было исправлено

✅ **Реальный вызов Node.js скрипта** вместо placeholder  
✅ **Подсчет результатов** из базы данных после миграции  
✅ **Правильная передача переменных** окружения  
✅ **Полный вывод ошибок** для отладки  

## Быстрый тест

### 1. Проверьте, что бэкенд запущен

```powershell
netstat -ano | findstr ":8080"
```

Должно показать процесс на порту 8080.

### 2. Откройте фронтенд

Перейдите в **Настройки → Миграция**

### 3. Введите данные AlfaCRM

```
URL: https://smarteducation.s20.online
Email: alimbaysuleymen@gmail.com
API Key: df9d5f7a-9b6a-11eb-ac2a-ac1f6b4782be
```

### 4. Нажмите "Проверить соединение"

Должно показать: ✅ **Соединение установлено**

### 5. Нажмите "Запустить миграцию"

- Не закрывайте страницу!
- Следите за прогресс-баром
- Процесс займет 2-5 минут

### 6. Проверьте результаты

После завершения вы должны увидеть:

```
✅ Миграция завершена!

• Учителей: 7
• Студентов: 262
• Групп: 53
• Аудиторий: ~12
• Уроков: ~172
```

## Если что-то пошло не так

### Ошибка: "node: command not found"

**Решение:** Установите Node.js

```powershell
# Проверьте установку
node --version

# Если не установлен, скачайте с https://nodejs.org/
```

### Ошибка: "migration script failed"

**Решение:** Проверьте логи

```powershell
# Посмотрите логи бэкенда
Get-Content C:\Users\Aldiyar\classmate-central-92\backend\error.log -Tail 50
```

### Ошибка: "Failed to connect to AlfaCRM"

**Решение:** Проверьте данные:
- URL должен начинаться с `https://`
- API ключ должен быть актуальным
- Проверьте интернет соединение

### Миграция завершилась, но счетчики 0

**Возможные причины:**
1. Скрипт Node.js не смог подключиться к AlfaCRM
2. Путь к скрипту неправильный
3. Не установлены зависимости Node.js

**Решение:**

```powershell
# Перейдите в папку migration
cd C:\Users\Aldiyar\classmate-central-92\migration

# Установите зависимости
npm install

# Проверьте что скрипт существует
dir migrate-from-alfacrm.js

# Попробуйте запустить вручную
node migrate-from-alfacrm.js
```

## Проверка данных в БД

После успешной миграции проверьте данные:

```powershell
cd C:\Users\Aldiyar\classmate-central-92\migration
node check-migrated-data.js
```

Должно показать статистику по вашей компании.

## Важно! 

⚠️ **Каждый пользователь мигрирует данные в свою компанию**

- User1 имеет данные в компании `default-company`
- Новый пользователь получит новую компанию автоматически
- Данные полностью изолированы

## Отладка

### Включите детальное логирование

В `backend/.env` добавьте:

```
LOG_LEVEL=debug
```

### Проверьте переменные окружения

```powershell
# В PowerShell
Get-ChildItem Env: | Where-Object {$_.Name -like "DB_*"}
```

### Ручной запуск миграции

```powershell
cd migration

# Установите переменные
$env:ALFACRM_API_URL = "https://smarteducation.s20.online"
$env:ALFACRM_EMAIL = "alimbaysuleymen@gmail.com"  
$env:ALFACRM_API_KEY = "df9d5f7a-9b6a-11eb-ac2a-ac1f6b4782be"
$env:COMPANY_ID = "your-company-id"
$env:DB_HOST = "localhost"
$env:DB_PORT = "5432"
$env:DB_NAME = "classmate_central"
$env:DB_USER = "postgres"
$env:DB_PASSWORD = "postgres"

# Запустите
node migrate-from-alfacrm.js
```

---

**Теперь миграция должна работать полностью!** 🚀

