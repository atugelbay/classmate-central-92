# 🎓 Classmate Central CRM

Полнофункциональная CRM система для управления образовательным центром с модулями управления лидами, финансами, абонементами и расписанием.

---

## ✨ Основные возможности

### 📚 Управление учебным процессом
- **Преподаватели**: Управление профилями преподавателей
- **Студенты**: База данных студентов с финансовой информацией
- **Группы**: Организация студентов в группы
- **Расписание**: Календарь занятий с drag-and-drop и просмотром по аудиториям

### 💼 Управление продажами (CRM)
- **Лиды**: Полный воронка продаж
- **Статусы**: new, in_progress, enrolled, rejected
- **Источники**: call, website, social, referral
- **Задачи**: Управление задачами по лидам
- **История активности**: Отслеживание всех взаимодействий
- **Конверсия**: Статистика по конверсии лидов

### 💰 Финансовый модуль
- **Транзакции**: Учет всех платежей и возвратов
- **Балансы студентов**: Отслеживание баланса каждого студента
- **Тарифы**: Управление ценами и пакетами
- **Долги**: Отслеживание и управление задолженностями

### 🎫 Система абонементов
- **Типы абонементов**: Гибкая настройка пакетов занятий
- **Заморозка**: Возможность заморозить абонемент
- **Срок действия**: Абонементы с ограниченным сроком
- **Автоматическое списание**: Учет посещений
- **Посещаемость**: Отслеживание посещений по занятиям

### 🏢 Управление помещениями
- **Аудитории**: Управление учебными помещениями
- **Расписание по аудиториям**: Визуализация занятости помещений
- **Вместимость**: Контроль загрузки аудиторий

---

## 🛠️ Технологический стек

### Backend
- **Go 1.21+** с фреймворком Gin
- **PostgreSQL 15** для хранения данных
- **RESTful API** с JWT аутентификацией
- **Database migrations** для версионирования схемы

### Frontend
- **React 18** с TypeScript
- **Vite** для быстрой разработки
- **React Query** для управления состоянием сервера
- **React Big Calendar** для календаря занятий
- **Shadcn/ui** компоненты с Tailwind CSS
- **React Router** для навигации
- **Axios** для HTTP запросов

---

## 🚀 Быстрый старт

### Требования
- Go 1.21+
- Node.js 18+
- PostgreSQL 15+
- Docker (опционально)

### Установка

#### 1. Клонирование репозитория
```bash
git clone https://github.com/ваш-username/classmate-central.git
cd classmate-central
```

#### 2. Запуск базы данных (Docker)
```bash
cd backend
docker-compose up -d
```

Или установите PostgreSQL вручную и создайте БД:
```sql
CREATE DATABASE classmate_central;
```

#### 3. Запуск Backend
```bash
cd backend
go mod download
go run cmd/api/main.go
```

Backend будет доступен на `http://localhost:8080`

#### 4. Запуск Frontend
```bash
cd frontend
npm install
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

#### 5. Первый вход
- Email: `admin@example.com`
- Password: `admin123`

---

## 📦 Деплой в продакшен

### Вариант 1: Railway.app (Рекомендуется)

Подробная инструкция в файле **[RAILWAY_DEPLOY.md](./RAILWAY_DEPLOY.md)**

**Быстрые шаги:**
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект из GitHub репозитория
3. Добавьте PostgreSQL базу
4. Настройте переменные окружения
5. Deploy! 🚀

### Вариант 2: VPS с Docker

```bash
# Клонируйте проект на сервере
git clone https://github.com/ваш-username/classmate-central.git
cd classmate-central

# Соберите и запустите контейнеры
docker-compose -f docker-compose.prod.yml up -d --build
```

### Вариант 3: Ngrok (для тестирования)

```bash
# Запустите backend локально
cd backend
go run cmd/api/main.go

# В другом терминале запустите ngrok
ngrok http 8080
```

---

## 🧪 Нагрузочное тестирование

Подробная инструкция в файле **[LOAD_TESTING.md](./LOAD_TESTING.md)**

```bash
# Установите k6
choco install k6

# Запустите тест
k6 run load-test.js

# Для Railway/Ngrok
k6 run -e API_URL=https://ваш-backend.railway.app load-test.js
```

---

## 📁 Структура проекта

```
classmate-central-92/
├── backend/
│   ├── cmd/api/           # Точка входа приложения
│   ├── internal/
│   │   ├── database/      # Подключение к БД
│   │   ├── handlers/      # HTTP handlers
│   │   ├── middleware/    # CORS, Auth middleware
│   │   ├── models/        # Data models
│   │   └── repository/    # Database queries
│   ├── migrations/        # SQL миграции
│   ├── Dockerfile         # Docker образ
│   └── go.mod
├── frontend/
│   ├── src/
│   │   ├── api/           # API clients
│   │   ├── components/    # React компоненты
│   │   ├── hooks/         # Custom hooks
│   │   ├── pages/         # Страницы приложения
│   │   └── types/         # TypeScript types
│   ├── Dockerfile         # Docker образ
│   └── package.json
├── load-test.js           # k6 нагрузочные тесты
├── railway.json           # Railway конфигурация
└── README.md
```

---

## 🔑 API Endpoints

### Authentication
```
POST   /api/auth/register    # Регистрация
POST   /api/auth/login       # Вход
POST   /api/auth/refresh     # Обновление токена
GET    /api/auth/me          # Текущий пользователь
```

### Teachers
```
GET    /api/teachers         # Список преподавателей
POST   /api/teachers         # Создать преподавателя
PUT    /api/teachers/:id     # Обновить преподавателя
DELETE /api/teachers/:id     # Удалить преподавателя
```

### Students
```
GET    /api/students         # Список студентов
POST   /api/students         # Создать студента
PUT    /api/students/:id     # Обновить студента
DELETE /api/students/:id     # Удалить студента
```

### Lessons
```
GET    /api/lessons          # Список уроков
POST   /api/lessons          # Создать урок
PUT    /api/lessons/:id      # Обновить урок
DELETE /api/lessons/:id      # Удалить урок
```

### Leads (CRM)
```
GET    /api/leads            # Список лидов
GET    /api/leads/stats      # Статистика конверсии
POST   /api/leads            # Создать лид
PUT    /api/leads/:id        # Обновить лид
GET    /api/leads/:id/activities  # История активности
POST   /api/leads/:id/activities  # Добавить активность
GET    /api/leads/:id/tasks       # Задачи по лиду
POST   /api/leads/:id/tasks       # Создать задачу
```

### Finance
```
GET    /api/payments/transactions         # Все транзакции
POST   /api/payments/transactions         # Создать транзакцию
GET    /api/payments/balance/:studentId   # Баланс студента
GET    /api/tariffs                       # Список тарифов
POST   /api/tariffs                       # Создать тариф
GET    /api/debts                         # Список долгов
POST   /api/debts                         # Создать запись о долге
```

### Subscriptions
```
GET    /api/subscriptions/types           # Типы абонементов
POST   /api/subscriptions/types           # Создать тип
GET    /api/subscriptions                 # Список абонементов
POST   /api/subscriptions                 # Выдать абонемент
GET    /api/subscriptions/:id/freezes     # История заморозок
POST   /api/subscriptions/:id/freezes     # Заморозить абонемент
POST   /api/attendance                    # Отметить посещение
```

---

## 🔒 Безопасность

- JWT токены для аутентификации
- Bcrypt для хеширования паролей
- CORS middleware для защиты от XSS
- SQL injection protection через prepared statements
- Rate limiting (рекомендуется настроить)

---

## 🤝 Разработка

### Добавление нового модуля

1. **Backend:**
   ```go
   // 1. Создайте модель в internal/models/
   // 2. Создайте repository в internal/repository/
   // 3. Создайте handler в internal/handlers/
   // 4. Зарегистрируйте routes в cmd/api/main.go
   ```

2. **Frontend:**
   ```typescript
   // 1. Добавьте TypeScript type в src/types/
   // 2. Создайте API client в src/api/
   // 3. Создайте React Query hooks в src/hooks/useData.ts
   // 4. Создайте страницу в src/pages/
   // 5. Добавьте роут в src/App.tsx
   ```

### Database Migrations

```bash
# Создайте новую миграцию в backend/migrations/
# Формат: XXX_название.up.sql и XXX_название.down.sql

# Миграции выполняются автоматически при запуске backend
```

---

## 📊 Мониторинг

### Логи
```bash
# Backend логи
cd backend
go run cmd/api/main.go

# Railway логи
# Доступны в Railway Dashboard → Deployments
```

### Метрики
- Количество запросов в логах
- Время выполнения запросов
- Ошибки HTTP (4xx, 5xx)
- Использование ресурсов (Railway Dashboard)

---

## 🐛 Troubleshooting

### Backend не запускается
```bash
# Проверьте подключение к БД
docker-compose ps

# Проверьте порт 8080
netstat -ano | findstr :8080

# Проверьте логи
go run cmd/api/main.go
```

### Frontend не подключается к API
```typescript
// Проверьте URL в frontend/src/api/client.ts
const apiClient = axios.create({
  baseURL: "http://localhost:8080", // Правильный URL?
});
```

### Миграции не применяются
```bash
# Проверьте connection string
# Убедитесь что PostgreSQL запущен
docker-compose logs postgres
```

---

## 📝 TODO / Roadmap

- [ ] Интеграция с платежными системами (Kaspi, PayBox)
- [ ] Email уведомления
- [ ] SMS уведомления (Telegram Bot)
- [ ] Экспорт отчетов в PDF/Excel
- [ ] Мобильное приложение
- [ ] Multi-tenancy (для нескольких филиалов)
- [ ] Роли и права доступа
- [ ] Аудит логирование

---

## 📄 Лицензия

MIT License - делайте что хотите!

---

## 👥 Контакты

Если есть вопросы или предложения, создайте Issue в GitHub.

---

**Сделано с ❤️ для образовательных центров Казахстана**

