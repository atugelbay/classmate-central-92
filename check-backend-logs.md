# 🔍 Проверь логи бэкенда

Посмотри в терминале где запущен backend (порт 8080).

Когда открываешь страницу "Учителя", должны быть строки типа:
```
[GIN] 2025/10/26 - 13:17:31 | 200 |      6.8601ms |             ::1 | GET      "/api/teachers"
```

Если видишь:
- `200` → запрос успешный
- `401` → проблема с авторизацией
- `403` → проблема с доступом
- `500` → ошибка на сервере

**Скопируй сюда все строки с `/api/teachers` из логов!**

