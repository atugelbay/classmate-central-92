# Быстрый старт: Новые настройки UI/UX

## 🎨 Что нового?

Страница настроек теперь включает полноценную вкладку **"Интерфейс"** с персонализацией!

## 📋 Структура вкладок

1. **Общие** - Настройки учебного центра
2. **Интерфейс** ⭐ НОВОЕ - UI/UX настройки
3. **Миграция** - Импорт из AlfaCRM

## 🎯 Возможности вкладки "Интерфейс"

### 🌓 Тема
- **Светлая** - классический светлый режим
- **Темная** - комфортный темный режим
- **Системная** - автоматически следует за системой

### 🎨 Цветовые палитры (6 вариантов)
- 🔵 Синий (по умолчанию)
- 🟣 Фиолетовый
- 🟢 Зеленый
- 🟠 Оранжевый
- 🔴 Красный
- 💗 Розовый

### 📐 Размер интерфейса
- **Компактный** - 14px, для больших экранов
- **Нормальный** - 16px, стандартный размер
- **Комфортный** - 18px, для удобного чтения

### ⚡ Анимации
- Включить/выключить все анимации и переходы

### 📊 Плотность данных
- **Компактная** - больше данных на экране
- **Стандартная** - баланс между данными и пробелами
- **Просторная** - увеличенные отступы

## 💾 Сохранение

Все настройки:
- ✓ Сохраняются автоматически в браузере
- ✓ Применяются мгновенно
- ✓ Сохраняются между сеансами
- ✓ Не требуют перезагрузки страницы

## 🔧 Для разработчиков

### Использование ThemeContext в компонентах

```tsx
import { useThemeContext } from "@/context/ThemeContext";

function MyComponent() {
  const { theme, uiPreferences, applyColorTheme } = useThemeContext();
  
  // Текущая тема: light, dark, system
  console.log(theme);
  
  // Настройки UI
  console.log(uiPreferences.colorTheme); // blue, purple, etc.
  console.log(uiPreferences.interfaceSize); // compact, normal, comfortable
  console.log(uiPreferences.animationsEnabled); // true/false
  console.log(uiPreferences.dataDensity); // compact, standard, spacious
  
  // Изменить палитру
  applyColorTheme("purple");
}
```

### CSS переменные
```css
/* Автоматически обновляются при смене палитры */
var(--primary)
var(--primary-light)
var(--primary-dark)
var(--primary-foreground)
```

### CSS классы
```css
/* Размер интерфейса */
html.ui-compact { font-size: 14px; }
html.ui-normal { font-size: 16px; }
html.ui-comfortable { font-size: 18px; }

/* Отключение анимаций */
html.reduce-motion * { animation-duration: 0.01ms !important; }

/* Плотность данных */
html.density-compact table td { padding: 0.25rem 0.5rem; }
html.density-standard table td { padding: 0.5rem 1rem; }
html.density-spacious table td { padding: 0.75rem 1.5rem; }
```

## 🗑️ Что удалено

- ❌ Вкладка "Аудитории" из настроек (функционал остался в Schedule и Groups)

## ✅ Что осталось без изменений

- ✓ Все настройки учебного центра
- ✓ Функционал миграции
- ✓ Управление аудиториями в других разделах
- ✓ Вся бизнес-логика приложения

## 🚀 Запуск

```bash
cd frontend
npm run dev
```

Откройте **Настройки → Интерфейс** и наслаждайтесь персонализацией! 🎉

