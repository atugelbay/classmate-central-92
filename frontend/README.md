# SmartCRM Frontend

React приложение для системы управления образовательным центром SmartCRM.

## 🛠 Tech Stack

- **React 18** + **TypeScript**
- **Vite** - быстрый сборщик
- **React Router** - маршрутизация
- **TanStack Query (React Query)** - управление серверным состоянием
- **Zustand** - управление клиентским состоянием
- **Tailwind CSS** - utility-first CSS фреймворк
- **shadcn/ui** - высококачественные UI компоненты
- **Axios** - HTTP клиент
- **date-fns** / **moment** - работа с датами
- **lucide-react** - иконки

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+
- npm или yarn
- Backend API запущен на `http://localhost:8080`

### Установка

1. **Установите зависимости:**
```bash
npm install
```

2. **Создайте файл `.env`:**
```bash
VITE_API_URL=http://localhost:8080/api
```

3. **Запустите dev сервер:**
```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

## 📜 Доступные скрипты

- `npm run dev` - Запуск development сервера с hot reload
- `npm run build` - Сборка для production
- `npm run preview` - Просмотр production сборки локально
- `npm run lint` - Запуск ESLint

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── api/                 # API клиенты
│   │   ├── client.ts        # Базовый Axios клиент
│   │   ├── auth.ts         # Аутентификация
│   │   ├── students.ts     # Студенты
│   │   ├── teachers.ts     # Преподаватели
│   │   ├── finance.ts      # Финансы
│   │   └── ...
│   │
│   ├── components/          # React компоненты
│   │   ├── ui/             # shadcn/ui компоненты
│   │   │   ├── button.tsx
│   │   │   ├── dialog.tsx
│   │   │   ├── table.tsx
│   │   │   └── ...
│   │   ├── Layout.tsx      # Основной layout
│   │   ├── AppSidebar.tsx  # Боковая панель
│   │   └── ...
│   │
│   ├── pages/               # Страницы приложения
│   │   ├── Dashboard.tsx   # Главная страница
│   │   ├── Students.tsx    # Список студентов
│   │   ├── StudentDetail.tsx # Детали студента
│   │   ├── Teachers.tsx
│   │   ├── Schedule.tsx
│   │   ├── Finance.tsx
│   │   └── ...
│   │
│   ├── hooks/              # Custom hooks
│   │   ├── useData.ts      # React Query hooks
│   │   ├── useConfirmDelete.ts
│   │   └── ...
│   │
│   ├── context/             # React Context
│   │   ├── AuthContext.tsx # Аутентификация
│   │   └── ThemeContext.tsx # Тема оформления
│   │
│   ├── store/              # Zustand store
│   │   └── useStore.ts
│   │
│   ├── types/               # TypeScript типы
│   │   └── index.ts
│   │
│   └── lib/                 # Утилиты
│       ├── utils.ts
│       └── cn.ts
│
├── public/                  # Статические файлы
├── index.html
├── vite.config.ts
├── tailwind.config.ts
└── tsconfig.json
```

## 🎨 Основные функции

### Аутентификация

- Регистрация и вход
- JWT токены с автоматическим обновлением
- Email верификация
- Принятие приглашений
- Protected routes (защищенные маршруты)
- Автоматический logout при истечении токена

### Управление данными

#### Студенты
- Список студентов с пагинацией и поиском
- Детальная страница студента
- Балансы и транзакции
- Абонементы
- История активности
- Заметки преподавателей
- Журнал посещаемости
- Уведомления

#### Преподаватели
- Список преподавателей
- Детальная страница преподавателя
- CRUD операции

#### Группы
- Список групп
- Создание и редактирование групп
- Автоматическая генерация уроков

#### Расписание
- Календарь занятий
- Групповые и индивидуальные уроки
- Создание и редактирование уроков
- Проверка конфликтов

#### Финансы
- Транзакции (платежи, возвраты, долги)
- Балансы студентов
- Тарифы и скидки
- Экспорт в PDF/Excel

#### Абонементы
- Список абонементов
- Создание и управление абонементами
- Заморозка абонементов
- Статистика использования

#### Дашборд
- Общая статистика
- Уроки на сегодня
- Графики доходов
- Статистика посещаемости

### Особенности UI/UX

- **Автоматическое кеширование** - React Query кеширует данные
- **Оптимистичные обновления** - мгновенный UI feedback
- **Loading states** - индикаторы загрузки
- **Error handling** - централизованная обработка ошибок
- **Empty states** - красивые пустые состояния
- **Confirmation dialogs** - подтверждение удаления
- **Responsive design** - адаптивный дизайн
- **Dark mode** - поддержка темной темы (через ThemeContext)

## 🔌 API Интеграция

Все API запросы проходят через централизованный клиент (`src/api/client.ts`):

- Автоматическое добавление JWT токенов в заголовки
- Автоматическое обновление токенов при истечении
- Обработка ошибок (401, 403, 500)
- Перехватчики запросов и ответов

### Пример использования

```typescript
import { useStudents } from '@/hooks/useData';

function StudentsPage() {
  const { data: students, isLoading } = useStudents();
  
  if (isLoading) return <LoadingState />;
  
  return (
    <div>
      {students?.map(student => (
        <StudentCard key={student.id} student={student} />
      ))}
    </div>
  );
}
```

## 🎯 React Query

Используется для управления серверным состоянием:

```typescript
// Автоматическое кеширование
const { data, isLoading, error } = useQuery({
  queryKey: ['students'],
  queryFn: () => studentsAPI.getAll(),
  staleTime: 5 * 60 * 1000, // 5 минут
});

// Мутации
const mutation = useMutation({
  mutationFn: studentsAPI.create,
  onSuccess: () => {
    queryClient.invalidateQueries(['students']);
  },
});
```

## 🎨 Стилизация

### Tailwind CSS

Используется utility-first подход:

```tsx
<div className="flex items-center justify-between p-4 bg-white rounded-lg shadow">
  <h2 className="text-xl font-semibold">Заголовок</h2>
</div>
```

### shadcn/ui

Компоненты из shadcn/ui используются как основа:

```tsx
import { Button } from '@/components/ui/button';
import { Dialog } from '@/components/ui/dialog';

<Button variant="default">Кнопка</Button>
<Dialog>
  <DialogContent>...</DialogContent>
</Dialog>
```

## 🔒 Права доступа (RBAC)

Навигация и функционал зависят от прав пользователя:

```typescript
const { user } = useAuth();
const hasPermission = user?.permissions?.includes('students:create');

{hasPermission && (
  <Button onClick={handleCreate}>Создать студента</Button>
)}
```

## 📱 Responsive Design

Приложение адаптивно и работает на:
- Desktop (1920px+)
- Laptop (1024px+)
- Tablet (768px+)
- Mobile (320px+)

## 🚀 Production Build

### Сборка

```bash
npm run build
```

Файлы будут в папке `dist/`

### Preview

```bash
npm run preview
```

### Развертывание

Файлы из `dist/` можно развернуть на любом статическом хостинге:

- **Vercel** - автоматический деплой из Git
- **Netlify** - автоматический деплой из Git
- **GitHub Pages** - статический хостинг
- **Nginx** - собственный сервер

### Docker

```bash
docker build -t smartcrm-frontend .
docker run -p 80:80 smartcrm-frontend
```

## 🔧 Environment Variables

### Development

```env
VITE_API_URL=http://localhost:8080/api
```

### Production

```env
VITE_API_URL=https://api.smartcrm.com/api
```

## 🧪 Тестирование

### Линтинг

```bash
npm run lint
```

### Type Checking

```bash
npx tsc --noEmit
```

## 📦 Основные зависимости

- `react` / `react-dom` - React библиотека
- `react-router-dom` - маршрутизация
- `@tanstack/react-query` - серверное состояние
- `zustand` - клиентское состояние
- `axios` - HTTP клиент
- `tailwindcss` - CSS фреймворк
- `lucide-react` - иконки
- `date-fns` - работа с датами
- `@radix-ui/*` - UI примитивы (через shadcn/ui)

## 🐛 Troubleshooting

### Frontend не может подключиться к API

1. Убедитесь что backend запущен на порту 8080
2. Проверьте `VITE_API_URL` в `.env`
3. Проверьте CORS настройки в backend

### Ошибка "Invalid token"

1. Очистите localStorage в браузере
2. Заново войдите в систему

### Стили не применяются

1. Убедитесь что Tailwind правильно настроен
2. Проверьте `tailwind.config.ts`
3. Перезапустите dev сервер

## 📚 Дополнительные ресурсы

- [React Documentation](https://react.dev/)
- [Vite Documentation](https://vitejs.dev/)
- [React Query Documentation](https://tanstack.com/query)
- [Tailwind CSS Documentation](https://tailwindcss.com/)
- [shadcn/ui Documentation](https://ui.shadcn.com/)

## 🤝 Contributing

1. Создайте feature branch
2. Внесите изменения
3. Убедитесь что код проходит линтинг
4. Создайте Pull Request

## 📝 License

MIT License
